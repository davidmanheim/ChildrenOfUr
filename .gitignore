# Downloaded reference archives (glitch-items, glitch-gameserver, browser-check) —
# large (~2GB+), not our code, re-fetchable; see CONTENT_RECOVERY_PLAN.md for provenance.
/tmp/

# Large binary drops kept local-only (not suited to a normal git history).
*.zip
*.crdownload

# Dart/Angular build artifacts — regenerated by `dart pub get`/build, not source.
.dart_tool/
build/
.packages
pubspec_overrides.yaml

# Editor/OS noise
.DS_Store
Thumbs.db

# Temporary staging area used while migrating coUserver/coUclient/authServer
# into git-subtree-merged forks; removed once the migration commit lands.
/coUserver.local/
/coUclient.local/
/authServer.local/
