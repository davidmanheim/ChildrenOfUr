# Local revival work tracker

This is the active implementation tracker. `POTENTIAL_TODO.md` remains a
separate list of review recommendations; items here are user-facing recovery
work that has been requested, observed, or is required to make a completed
feature genuinely usable.

Legend: **done** = implemented and smoke-tested; **active** = currently being
implemented/verified; **blocked** = needs a source or decision; **planned** =
accepted next work, not yet started.

## Runtime stability

| Status | Work | Evidence / next verification |
| --- | --- | --- |
| done | Local auth and local session | `LocalPlayer` can launch and travel through streets. Keep hosted Firebase path separate from local mode. |
| done | Server crash containment and logging | Latest game server starts and survives reconnects; Docker logs are the diagnosis surface. |
| done | Initial Dart/browser compatibility fixes | Number formatting, stale API calls, gamepad binding, timer/platform calls, and several mapper failures fixed as encountered. Continue regression smoke testing. |
| active | Map visited-street display | Verified 2026-08-27: server persists L-form TSIDs (18 rows for LocalPlayer in `metabolics.location_history`), and the null-history normalization is compiled into the served client bundle. Remaining: in-browser check of Tonga Trips/Pitika Parse/Jolkan Jank map highlighting. |
| active | Quoin feedback and reward clarity | Per-type visual tints added in `coUclient/web/files/css/desktop/quoins.css` (keyed on the existing type class from `Quoin.init`); denied quoins are now re-shown instead of staying invisible, and the collect handler no longer crashes on late responses (`metabolics_service.dart:collectQuoin`). Remaining: in-browser check of tint colors and meter/text response. |
| active | Transparent quoin rendering | Verified 2026-08-27: server sends `files/sprites/generated/local-quoin.svg` (running container confirmed) and the client serves it as `image/svg+xml`. Remaining: in-browser visual confirmation. |
| done | Buff persistence and buff-error loop | `PlayerBuff._write` failed on every call (`Map<String, dynamic>` assigned to `Map<String, int>`), so buff timers never persisted and the log filled with an error every 10s; fixed with an explicit `.cast<String, int>()` plus a real cache removal in `PlayerBuff.remove`. Post-restart logs are clean. |
| done | Server status metrics and weather noise | `bytesUsed` now uses `ProcessInfo.currentRss` (the image lacks `bc`/`ps`, so the shell scripts always failed); CPU degrade is silent; weather polling is skipped entirely when `openWeatherMap` is empty instead of logging a guaranteed 401 per street. Verified via `/serverStatus` returning a real RSS value with zero post-restart ERROR lines. |
| done | NPC/item interaction crash chain | Placing real entities in the world (see World data below) surfaced 6 previously-latent crashes along the full interact→act→reward path, each confirmed live and fixed: `Entity.getMap()`'s untyped-map downcast (crashed on every simulate tick with an occupant present), Chicken/Scarecrow's `messageBus.subscribe` closures typed too narrowly for reflective construction (blocked Chicken from ever loading), Chicken/Piggy `restoreState` casting `jsonDecode` output directly into typed List/Map fields, the client's generated `Action.toJson()` never calling nested `.toJson()` (broke every item-gated interaction — fixed in hand-written `itemdef.dart` since `itemdef.g.dart` is regenerated by build_runner every build), `Item.actionList`'s untyped-list downcast, and vendored `jsonx`'s `_jsonToObject` calling `.isEnum` on a `dynamic`-field's type mirror (crashed granting any item with metadata). End-to-end verified: chicken interaction menu, Squeeze action, and a real reward grant all complete with the server staying up. |
| done | Game-server crash: null log-setting boolean | Found via autonomous check: the running game-server container had exited (255) ~12 min after its last restart. Root cause: `Log.log` (`coUserver/lib/common/log/log.dart`) used `LogSettings.getBool(...)` directly in boolean `if` conditions with no fallback; a read returning null (e.g. a transient/incomplete `logsettings.json` parse) threw `Failed assertion: boolean expression must not be null` from inside `FileCache`'s periodic disk-cache-save `Timer`, killing the whole process. Fixed with explicit true/true/false fallbacks. Rebuilt and verified: container stable, `/serverStatus` returns 200. |
| done | Auth logout/verification correctness | `SESSIONS.remove([token])` (List-as-key no-op), URL-encoded email used in the verification dedupe SELECT, and the `email_verification` table-name typo are fixed in `authServer/lib/auth.dart`; raw mapper query results there and across coUserver (auctions, mailbox, notes, users, skills, buffs, instancing, migrations) now cast to typed lists. Smoke-tested: localSession -> logout round-trip returns ok. |
| done | Harvest/interact crash chain, round 2 | Found live (user report: harvest gave nothing, plus image-load 500s): a second, deeper instance of the jsonx `_jsonToObject` bug (`.newInstance` called unconditionally on a `dynamic`-field's type mirror, not just `.isEnum` -- broadened the guard); `getActualImageHeight` tried `http.get()` on local sprites' relative paths, which was never valid and can't reach the client container's files over HTTP anyway -- added a read-only volume mount and read local files directly. Systematic sweep (per explicit instruction to fix the whole class, not one symptom) found and fixed 7 more instances of the same narrow-closure/untyped-collection patterns across `stats.dart`, `Tree.pet`, `DustTrap`, `playerskill.dart`, `achievements.dart`, `visited.dart`, and `mailbox.dart` (a `Map` assigned into a `String` field, breaking every mailed item attachment). Separately: `NPC` shadowed `Entity`'s `chatBubble` field, breaking dynamic dispatch for every NPC bubble timeout (invisible until enough NPCs were actually talking). All verified live: Bean Tree Pet/Water/Harvest all complete cleanly, harvested item confirmed in the DB `inventory_json`, zero console errors post-fix vs. 183+ before. |

## World data and content

| Status | Work | Evidence / next verification |
| --- | --- | --- |
| done | Geometry/world map | CAT422 map data loads; travel across streets works. |
| done | Synthetic placement seed | `tools/seed-demo-world.mjs` has deterministic geometry-aware quoin rows for all 3,180 unique map TSIDs. These are explicitly synthetic (`demo-` IDs). |
| done | Environmental entities | 38 real-art entity types now seeded (see "Continue asset conversion" below), not just quoins. Placement was redesigned 2026-08-28 from "one of everything on every street" (user feedback: "very repetitive") to a per-street rarity system themed by region (`tools/seed-demo-world.mjs`'s `RARITY`/`REGION_THEME`, reusing the hub/street "music" biome data recovered for the world-music pass) -- e.g. caverns favor rock/fish, bogs favor bubble tree, Ix/Hell suppress ordinary life while favoring the rare silver fox. Also fixed: ground-anchored entities (trees, rocks, animals) were floating up to ~275px in mid-air (the quoin hover-height formula was applied to every type uniformly); they now sit on the actual ground line. 4 more entity types (`HoochRespawningItem`/`PurpleFlowerRespawningItem`/`Still`/`Crab`) placed in a later same-day pass -- see the "Placement pass: hooch/purple_flower/crabato_juice" row below; only `Crab` of the 4 has real converted world-entity art, the other 3 place real working game logic with a still-dead-linked on-map sprite. |
| done | Authentic historic placements — decision | Decided 2026-08-27: not pursuing recovery of the original `street_entities`/MapFiller export. Generated/synthetic `demo-` placement (see `CONTENT_RECOVERY_PLAN.md`) is the permanent design, not an interim measure. |
| done | Official visual-asset archive (first 4 assets) | Chicken, wood tree, piggy, and a metal rock converted with real art via the JPEXS-FFDec SWF→PNG pipeline (`tools/README-swf-pipeline.md`), wired into their `coUserver` entity classes, **and placed in the world** (`tools/seed-demo-world.mjs` seeds one of each per street TSID, upgrading the retired DemoTree/DemoChicken placeholder slots in place). Verified live in-browser: renders correctly, right-click → Interact → Squeeze/Incubate/etc. works, server stays up. Full `tinyspeck/glitch-items` archive (~0.76 GB, ~5400 files) remains otherwise unconverted — see the "Continue asset conversion" row below. |
| active | Continue asset conversion | 133 converted source assets so far (37 world-entity animations + 24 world-entity-reward item icons + 12 first-batch standalone player-item icons + 13 second-batch croppery-gardening item icons + 16 third-batch spice item icons + 15 fourth-batch drink item icons, `content/source-manifest.json`), covering 38 world-entity types — all 38 placed in the world (up from 14/4) — plus 109 runtime-manifest rows. Batches: chicken/wood tree/piggy/metal rock; fox/silver fox/heli kitty/salmon/butterfly/batterfly/bean/bubble/fruit/paper trees; 11 giant shrines (Alph/Cosma/Friendly/Grendaline/Humbaba/Lem/Mab/Pot/Spriggan/Tii/Zille) + beryl/dullite/sparkly rock + dirt pile/ice nubbin/jellisac/mortar barnacle/peat bog + egg/gas/spice plant. Fruit tree's per-stage art and butterfly's 4-state rescale are documented simplifications/bugfixes (see `content/source-manifest.json`). Deferred with reasons in `runtime-manifest.json`: caterpillar, fox_bait, salmon_bubble icon, and the 3 regional shrine reskins (only the Ix variant was used, uniformly, for all 11 giants — documented, not per-giant guesswork). First standalone player-item batch (2026-08-28, not tied to any world-entity harvest) — apple, banana, honey, bacon (food), hoe, shovel, pick, scraper, watering_can (tools), dice, camera (toys), potion_rainbow_juice (tinctures & potions). 10 of the 12 have a genuine pre-existing acquisition route already in `coUserver` (apple/banana are `fruit_changing_machine` recipe outputs from Cherry; honey and the 5 tools are stocked by real, pre-existing vendor NPC classes — `ToolVendor` and `StreetSpirit`'s per-street `vendorType` data in `vendors.json` — though neither vendor class is placed/spawned in the live world yet, a separate future item; dice/camera are StreetSpirit "toy"-category stock). bacon and potion_rainbow_juice have no current vendor/recipe/reward route in the codebase at all — documented honestly as such rather than inventing one. All 5 converted tools reuse a single frame (the source SWF's actual content is a 33-57 frame tool-use animation, not an icon+stack-quantity sequence) for both iconUrl/spriteUrl. SECOND batch (same day) — all 13 `croppery-gardening.json` crops that had matching source SWFs: broccoli, cabbage, carrot, corn, cucumber, onion, parsnip, potato, pumpkin, rice, spinach, tomato, zucchini; same frame-1-icon/frames-2-5-stack-strip pattern as apple/banana, verified zero embedded bitmaps in each source SWF. All 13 share a real, unmodified harvest route: `Garden.CROPS` (`coUserver/lib/entities/npcs/garden.dart`) lists exactly these 13 crop names and its `harvest()` method really grants the matching item via `InventoryV2.addItemToUser`; 12 of the 13 (all but pumpkin, which the vendor stock list omits) are *also* stocked by the real, unmodified `produce` `StreetSpirit` vendor category. As with `ToolVendor`/`StreetSpirit` before, neither the `Garden` NPC class nor `StreetSpirit` is currently placed/spawned anywhere in the live world (confirmed by grep — no seed data references either), so both routes are real but currently inert; documented as such rather than claimed as live. THIRD batch (same day) — all 16 non-`allspice` `spices.json` items: black_pepper, camphor, cardamom, cinnamon, cumin, curry, garlic, ginger, hot_pepper, licorice, mustard, nutmeg, older_spice, pinch_of_salt, saffron, turmeric; same frame-1-icon/frames-2-5-stack-strip pattern (all 16 source SWFs are 5-frame `DefineSceneAndFrameLabelData`-encoded timelines, the same family convention as the already-converted `allspice`; verified zero top-level-frame-count surprises even where a few sources — camphor/ginger/nutmeg/pinch_of_salt — carry unusually high nested-shape/bitmap tag counts, confirmed to be nested detail graphics within the same 5 poses, not extra states). Re-verified directly in this pass (not just trusted from the prior note that found it): all 16 are real, unmodified `spice_mill` recipe outputs consuming `allspice` (`coUserver/lib/entities/items/actions/recipes/json/spice_mill.json`). Nuance beyond the prior batches' "route real but inert" finding: the *input* to this recipe, `allspice`, is actually already live-obtainable — `SpicePlant` (`coUserver/lib/entities/plants/trees/spiceplant.dart`), whose `harvest()` grants `allspice`, **is** placed in the live world by `tools/seed-demo-world.mjs` (`RARITY.SpicePlant`: 45% chance per street, maxCount 2, plus a regional boost on uralia2/kajuu streets — confirmed by reading the seed script directly). What is *not* live is the `spice_mill` tool itself needed to run the recipe: its only two acquisition routes, `ToolVendor.itemsForSale` and `StreetSpirit`'s `kitchen` vendor-stock category (109 streets in `vendors.json`), both confirmed inert by the same placement check the last two batches did — neither `ToolVendor` nor `StreetSpirit` (nor any regional subclass) is instantiated anywhere outside its own class definition. So this batch's route is genuinely closer to live than a plain "found but inert" — one hop (harvesting allspice) already works, the second hop (owning a spice mill) is the actual blocker. FOURTH batch (2026-08-28, after the vendor/harvest NPC placement pass below) — 15 `drinks.json` items with matching source SWFs under `tmp/glitch-items/drink/`: beer, coffee, earthshaker, face_smelter, flaming_humbaba, bubble_tea, cloud_11_smoothie, exotic_juice, fruity_juice, orange_juice, carrot_margarita, cosma_politan (source folder `cosmapolitan`, an id/folder-name mismatch like `butterfly_milk`/`milk_butterfly`), pumpkin_ale, hooch, crabato_juice; same 5-frame `DefineSceneAndFrameLabelData` family, re-verified per-file this pass. Because this batch was converted *after* `Garden`/`ToolVendor`/the 3 `StreetSpirit` subclasses were actually placed (see the row below), several routes are now genuinely live rather than merely wired-but-inert: beer and coffee are direct `StreetSpirit` `groceries`-category stock (86/865 streets); earthshaker, face_smelter, and flaming_humbaba are direct `StreetSpirit` `mining`-category stock (42/865 streets) *and* recipe outputs; bubble_tea, cloud_11_smoothie, exotic_juice, fruity_juice, and orange_juice are `blender` recipe outputs; carrot_margarita, cosma_politan, and pumpkin_ale are `cocktail_shaker` recipe outputs — `blender`/`cocktail_shaker`/`saucepan` are all real `kitchen`-category `StreetSpirit` stock (109/865 streets), so owning the tool is now reachable the same way `spice_mill` is. pumpkin_ale's full ingredient chain (beer, cinnamon, pumpkin) traces end-to-end to already-confirmed-live sources. Two items in this batch, by contrast, found a genuine gap rather than an inert-but-real route: `hooch` has two real, unmodified, but entirely unplaced acquisition routes (`HoochRespawningItem`, a `RespawningItem` world-entity harvestable; `Still`, an NPC-item that ferments corn/grain/potato/rice into hooch) — neither class appears anywhere in `tools/seed-demo-world.mjs`. `crabato_juice` likewise has one real but unplaced route: `Crab`'s `playMusic()` reward chain (`coUserver/lib/entities/npcs/crab.dart`) grants it when a player plays a musicblock the crab "likes." Both were documented as real-but-inert at the time; see the "Placement pass: hooch/purple_flower/crabato_juice" row below — all three are now placed. FIFTH batch (2026-08-28) — all 7 `herbalism.json` herbs (gandlevery, hairball_flower, purple_flower, rookswort, rubeweed, silvertongue, yellow_crumb_flower) plus their 7 matching seed items, all 14 with matching source SWFs under `tmp/glitch-items/herbs/` and `tmp/glitch-items/seeds/herb_seed_*/`; same 5-frame `DefineSceneAndFrameLabelData` family, re-verified per-file via `ffdec -dumpSWF`. This batch surfaced a genuine gameplay-code gap, not just a placement gap: `HerbGarden` (`coUserver/lib/entities/npcs/garden.dart` line 556) extends `Garden` and has its own herb-flavored flavor text (`RESPONSES_HERB`), strongly implying it should grow herbs, but it never overrides `Garden.CROPS` or `ITEM_REQ_PLANT` — both stay hardcoded to the 13 vegetable crops — so even if `HerbGarden` were placed in the world it could not actually plant or harvest any herb. Checked every vendor category in `vendor.dart`/`vendors.json` directly: no `herbalism` category exists at all (only `gardening`, which sells the 13 crop seeds, and `produce`, which sells the 13 crops). 6 of the 7 herbs (all but `purple_flower`) and all 7 seeds therefore have **no acquisition route of any kind** in the current codebase — not merely unplaced, genuinely unwritten, same category as `bacon`/`potion_rainbow_juice`. The 7th, `purple_flower`, is the exception: `PurpleFlowerRespawningItem` (`coUserver/lib/entities/plants/respawning_items/purple_flower.dart`) is a real, unmodified `RespawningItem` world-entity harvestable (3-minute respawn) — same shape as `hooch`'s `HoochRespawningItem` — that was not referenced anywhere in `tools/seed-demo-world.mjs` at the time; now placed, see the "Placement pass: hooch/purple_flower/crabato_juice" row below. All 7 herbs (including `purple_flower`) are real `tincturing_kit` recipe *inputs* (`essence_of_<herb>`, `coUserver/lib/entities/items/actions/recipes/json/tincturing_kit.json`) but never outputs, so that recipe file doesn't provide an acquisition route either. Item registry now has 94/788 (11.9%) items with real converted icon/sprite art (up from 80/788, 10.2%, directly re-verified by grepping every `iconUrl` field across `coUserver/lib/entities/items/json/*.json` for non-`childrenofur.com`, non-empty values — 673 items still point at dead `childrenofur.com` links, 21 have an empty `iconUrl`). Next candidates: remaining `inhabitants/` (crab, ghost, sloth, vendors, etc.), the rest of `harvestable_resources/`/other item icons, the ~330 remaining unconverted food items, ~36 remaining unconverted tools, `herdkeeping.json`'s remaining unconverted items, and 13 remaining unconverted drinks (11 with matching source SWFs: cloudberry_daiquiri, creamy_martini, gurly_drink, lemon_juice, mabbish_coffee, mega_healthy_veggie_juice, pungent_sunrise, savory_smoothie, slow_gin_fizz, spicy_grog, tooberry_shake). SIXTH batch (2026-08-28, scoped to exactly `storage.json`/`advanced_resources.json`/`basic_resources.json`/`machines-fuel.json` to avoid colliding with other agents converting other categories in parallel) — 14 items: 9 from `advanced_resources.json` (barnacle_talc, bulb, copper, general_fabric, ingot [item id for "Plain Metal Ingot"], molybdenum, string, thread, tin), 3 from `storage.json` (alchemistry_kit, firefly_jar, spicerack), and 2 from `machines-fuel.json` (fuel_cell, machine_stand); all matching source SWFs found directly under `tmp/glitch-items/misc/` and `tmp/glitch-items/tools/machine_stand/`. Frame pattern: 10 items are the standard 5-frame icon+4-stack pattern (frame 1 = icon, frames 2-5 = stack strip, visually confirmed); alchemistry_kit and firefly_jar have genuinely 1-frame source timelines (single static image reused for both iconUrl/spriteUrl, same convention as the `hoe`-family tools); spicerack's 3-frame source and machine_stand's 46-frame rotating-showcase timeline each had only frame 1 used for both outputs (documented simplification, same as the tool-animation precedent). This batch surfaced a real, substantial crafting chain, not just single-item vendor stock: `ingot` (`smelter` recipe from `chunk_metal`), `copper`/`tin`/`molybdenum` (`alchemical_tongs` recipes from `ingot` + red/green/blue/shiny elements, themselves `grinder` outputs from `chunk_dullite`/`chunk_beryl`/`chunk_sparkly` + `cherry`), `thread` (`spindle` from `fiber`), `string`/`general_fabric` (`loomer` from `thread`), and `barnacle_talc` (`grinder` from `barnacle`) are ALL real, unmodified recipe outputs whose raw-material harvest sources (MetalRock, DulliteRock, BerylRock, SparklyRock, Fox, MortarBarnacle, FruitTree) and required tools (smelter, alchemical_tongs, grinder, spindle, loomer) are every one of them already placed in the live world (mining-rocks/Fox/FruitTree entity batches plus the `ToolVendor`/`StreetSpirit` vendor placement pass) — genuinely live, end-to-end reachable, not wired-but-inert. `bulb`, `machine_stand`, `alchemistry_kit`, `firefly_jar`, and `spicerack` are real, unmodified, live `ToolVendor`/`StreetSpirit` vendor stock (the last three doubly so: explicitly named on their respective vendor lists *and* automatically covered by `vendor.dart`'s `pickItems(["Storage"])` helper, which stocks every item whose `category` field is `"Storage"` — i.e. this generic mechanism alone already covers all 19 `storage.json` items on `ToolVendor` and the `hardware` `StreetSpirit` category, whether or not each is individually named). `fuel_cell` was checked against every recipe file and every vendor list and found to have **no acquisition route of any kind** — same genuinely-missing-design category as `bacon`/`potion_rainbow_juice`. `basic_resources.json`'s only remaining unconverted item, `salmon_bubble`, has no matching source SWF anywhere under `tmp/glitch-items` (searched directly) and was left as-is. `storage.json`'s remaining 16 items and `advanced_resources.json`'s remaining 10 items (beam, board, girder, metal_bar, metal_rod, plain_crystal, snail, urth_block, wood_post, bushel_of_grain — confirmed to have no recipe/vendor/achievement route at all) and `machines-fuel.json`'s remaining 9 items (all confirmed real `ToolVendor`/`StreetSpirit(hardware)` stock, same live route as `machine_stand`) all have real matching source SWFs and are candidates for a future pass. Item registry now has 124/788 (15.7%) items with real converted icon/sprite art, directly re-verified by grepping every `iconUrl` field across `coUserver/lib/entities/items/json/*.json` for non-`childrenofur.com`, non-empty values this pass (643 items still point at dead `childrenofur.com` links, 21 have an empty `iconUrl`) — this total reflects this batch's 14 items plus other agents' concurrent conversions of other item categories in the same session, per the parallel-work note at the top of this task. SEVENTH batch (2026-08-28, scoped to exactly `alchemical.json`/`collectibles.json`/`licenses-permits.json`/`tinctures-potions.json` to avoid colliding with other agents converting other categories in parallel) — 16 items: abbasidose, blue_element, fertilidust (`alchemical.json`); potion_amorous_philtre, potion_ancestral_spirits, potion_tree_poison, essence_of_gandlevery (`tinctures-potions.json`, alongside the already-converted potion_rainbow_juice); card_carrying_qualification, fox_permit, general_building_permit, teleportation_script_imbued, teleportation_script, your_papers, note (all 7 of `licenses-permits.json`); wooden_apple, magical_pendant (`collectibles.json`); source SWFs from `tmp/glitch-items/compounds/`, `alchemy/`, `misc/`, and `artifacts/`. This batch's key finding, corrected after an initial mistake: an early pass checked only `test_tube.json`/`beaker.json`/`alchemical_tongs.json`/`tincturing_kit.json` for `red_element`/`green_element`/`blue_element`/`shiny_element` and (wrongly) concluded they had no acquisition route at all; re-checking `grinder.json` (missed the first time, but already known to matter from the SIXTH batch's own `copper`/`tin`/`molybdenum` note above) found real `blue_element-grinder`/`green_element-grinder`/`red_element-grinder`/`shiny_element-grinder` recipes producing them from `chunk_dullite`/`chunk_beryl`/`chunk_sparkly` (+ `cherry` for red) — the exact same rock/fruit harvests (DulliteRock/BerylRock/SparklyRock/FruitTree) already confirmed placed in `tools/seed-demo-world.mjs` by that SIXTH-batch chain. So all 4 elements, and therefore `abbasidose` (`test_tube`) and `fertilidust` (`beaker`, via the element-derived compounds `humbabol`/`potoxin`/`cosmox`), are genuinely live end-to-end — multi-hop (harvest rock/fruit → grind → test_tube → beaker) but real, not blocked. `content/runtime-manifest.json`'s entries for these 3 items were corrected to reflect this before this pass finished; the source-manifest conversion metadata was unaffected (accessibility only). `essence_of_gandlevery` (real `tincturing_kit` recipe, tool also placed) is blocked by two previously-documented gaps at once (`gandlevery` the herb has no route at all; `hooch` was real-but-unplaced, now placed per the row above — worth re-checking live). `card_carrying_qualification`'s only other reference is a same-named achievement in `player.json`, but the achievements subsystem never grants items anywhere (confirmed by grep), so it grants nothing. `fox_permit`, `general_building_permit`, `teleportation_script_imbued`, `teleportation_script`, `your_papers`, `wooden_apple`, `magical_pendant`, `potion_amorous_philtre`, `potion_ancestral_spirits`, and `potion_tree_poison` (the drinkable potion, distinct from `alchemical.json`'s `old_tree_poison` reagent) all have **no acquisition route of any kind** — checked every vendor list and recipe file directly. The one exception: `note` has a real, live, working route — `NoteManager.appEdit` (`coUserver/lib/entities/items/actions/note.dart`) consumes 1 `paper` and grants a `note` via a real HTTP route, auto-learning `penpersonship`; `paper` comes from `PaperTree`, which is one of the 38 world-entity types already placed by `tools/seed-demo-world.mjs` (confirmed by grep) — so `note` is reachable end-to-end today. Item registry now has 124/788 (15.7%) items with real converted icon/sprite art, directly re-verified by grepping every `iconUrl` field across `coUserver/lib/entities/items/json/*.json` this pass (still 21 with an empty `iconUrl`; the remaining 643 point at dead `childrenofur.com` links) — this figure already includes this SEVENTH batch's 16 items (the grep was run after these edits landed) alongside the SIXTH batch's 14 and prior batches; 94(baseline)+14(sixth)+16(seventh)=124 reconciles exactly. EIGHTH batch (2026-08-28, scoped to exactly `gasses-bubbles.json`/`herdkeeping.json`/`emblems-icons.json`/`quest_items.json` to avoid colliding with other agents converting other categories in parallel) — 16 items: all 8 of `gasses-bubbles.json` (bubble_blue, bubble_hard, bubble_tiny, gas_crying, gas_heavy, gas_laughing, gas_white, helium); 3 of `herdkeeping.json` (chick, piglet, caterpillar); 3 of `emblems-icons.json` (emblem_of_alph, emblem_of_cosma, icon_of_alph); 2 of `quest_items.json` (juju_paperweight, contraband). Source SWFs from `tmp/glitch-items/{food,misc,inhabitants,giants}/`; bubble_blue/hard/tiny have an id/folder mismatch (source folders `blue_bubble`/`hard_bubble`/`tiny_bubble`), same family as the earlier cosmapolitan/cosma_politan precedent. **Found and fixed a new instance of the systemic opaque-background ffdec defect** (distinct root cause from the metal-rock bug a parallel agent fixed the same day, per the "Live bug triage" row below, which used `-ignorebackground`): 13 of the 16 items' source SWFs render their art directly on the main timeline (not inside a named `DefineSprite`), and `ffdec -export frame` on those always composited an opaque SetBackgroundColor backdrop (alpha extrema exactly (255,255) on every one, confirmed via PIL) — `-config lastExportTransparentBackground=true` was tried first and confirmed to have no effect on `-export frame` output. Traced the actual cause via the equivalent `-format frame:svg` export, which showed an explicit `<rect fill="#ffffff".../>` background element baked into the vector output. Fixed with a new two-part tool (`tools/swf-patch-bgcolor.py` + `tools/swf-export-frame-alpha.py`, both new this pass): byte-patch a copy of the SWF's SetBackgroundColor tag to black, export the same frame against both white and black backdrops, then solve the standard two-backdrop difference-matting equations per pixel (`alpha = 1 - (C_white - C_black)/255`) to recover true anti-aliased alpha — re-verified alpha extrema (0,255) with a real intermediate-value distribution (not a binary mask) on all 43 frames this produced, and visually spot-checked 8 of them composited over a checkerboard. The other 3 items (chick, piglet, caterpillar) are herdkeeping "baby animal" items whose real art lives in a nested `DefineSprite` (`-export sprite`, chid 11/20/19 respectively) rather than the main timeline; that export path already renders with genuine alpha with no matting needed (verified directly), and only their single 'sit'-labeled frame 1 was used for both iconUrl and spriteUrl, matching their original source data where iconUrl and spriteUrl were already both the same flat image (`Chick.png`/`Piglet.png`/`Caterpillar.png`) rather than distinct icon/stack-strip frames; their separate `animSpriteUrl` field (the multi-hundred-frame walk/hop animation) was out of scope and still points at a dead link. Accessibility findings, each individually re-verified against the actual Dart source rather than assumed from category: all 8 `gasses-bubbles.json` items are real, unmodified, live end-to-end recipe outputs — bubble_blue/hard/tiny from `bubble_tuner.json` (input `plain_bubble`, `BubbleTree`'s harvest reward, `BubbleTree` already placed) and gas_crying/heavy/laughing/white/helium from `gassifier.json` (input `general_vapour`, `GasPlant`'s harvest reward, `GasPlant` already placed); both tools are real `ToolVendor`/`StreetSpirit` stock, both already placed (2026-08-28 vendor/harvest NPC placement pass). chick/piglet/caterpillar are real, unmodified, live multi-hop routes freshly traced end-to-end: `Chicken.incubate2()` (`coUserver/lib/entities/npcs/animals/chicken.dart`'s `EGG_ANIMALS` map) consumes 1 chicken_egg/piggy_egg/butterfly_egg and directly grants the baby-animal item via `InventoryV2.addItemToUser` (not a spawn); the 3 eggs are themselves real `egg_seasoner.json` recipe outputs whose ingredients (egg from the already-placed `EggPlant`; garlic from the already-placed spice chain; gas_white/bubble_hard/bubble_blue/bubble_tiny from this same batch's own now-live gasses-bubbles chain; potato/apple from the already-placed `Garden`/fruit-machine chains; honey from `ToolVendor` stock) all trace to already-live sources — `egg_seasoner` itself is real `ToolVendor` stock, already placed. emblem_of_alph and emblem_of_cosma are real, unmodified, live routes via `Shrine.donate()` (`coUserver/lib/entities/npcs/shrines/shrine.dart`): donating any priced item to a placed giant Shrine (all 11 already placed, prior batch) adds giant favor via `Metabolics._setFavor`, and crossing the favor-max threshold grants the emblem directly. icon_of_alph chains onto emblem_of_alph via the real, unmodified `Iconize` item action (`Emblem.iconize()`, `coUserver/lib/entities/items/actions/itemgroups/emblems-icons.dart`), which takes 11 emblem_of_alph and grants 1 icon_of_alph. juju_paperweight and contraband, by contrast, were checked against every recipe file, every vendor stock list/category (including the generic `pickItems(["Storage"])` mechanism, which doesn't cover "Quest Items"), every NPC harvest reward, and every quest/achievement definition, and found to have **no acquisition route of any kind** — same genuinely-missing-design category as `bacon`/`potion_rainbow_juice`/`your_papers`; both are quest-flavored by description (Juju Bandits/smuggler questlines) but no quest data implementing either questline exists in `coUserver`. Item registry now has 193/788 (24.5%) items with real converted icon/sprite art, directly re-verified by grepping every `iconUrl` field across `coUserver/lib/entities/items/json/*.json` this pass (575 items still point at dead `childrenofur.com` links, 20 have an empty `iconUrl`) — this figure includes this EIGHTH batch's 16 items plus other agents' concurrent conversions of other item categories in the same session (124 baseline + 16 this batch = 140, vs. 193 actual, meaning +53 items landed from parallel agents' work during this pass). NINTH batch (2026-08-28, scoped to exactly `keys.json`/`misc.json` -- the last two previously-entirely-untouched item-registry categories) -- converted all 12 `keys.json` door keys and 41 of `misc.json`'s 42 items (all but `new_player_pack_butterfly`, whose iconUrl referenced `newplayerbutterflybox.png`, a source SWF that does not exist anywhere under `tmp/glitch-items` -- searched directly). Hit the same opaque-background ffdec defect documented in the EIGHTH batch row above (confirmed via PIL alpha extrema == (255,255) on the first test conversions) and fixed it the same way, reusing the now-established `tools/swf-patch-bgcolor.py` + `tools/swf-export-frame-alpha.py` difference-matting tools rather than re-deriving a fix; one item (`crafty_bot`) additionally needed its own SetBackgroundColor patched to white first, since its source SWF's real background was `(102,153,153)`, not white (the tool's white-pass assumption) -- verified via a direct SetBackgroundColor-tag decode across all 53 sources that it was the only non-white one. All 106 final on-disk icon/sprite PNGs (53 items x 2) re-verified post-placement to have genuine, non-opaque alpha. Accessibility, checked individually against real Dart/JSON game logic rather than assumed: all 12 keys have **no acquisition route of any kind** -- 11 have no matching door/consumer class at all, and the 1 that does (`teal_white_triangle_key` / `TealWhiteTriangleLockedDoor`) is itself never instantiated anywhere. This pass also found the entire achievement system (`coUserver/lib/achievements/`) has **zero item-grant code anywhere** (`achievements.dart`/`achievement_checkers.dart` never call `InventoryV2.addItemToUser`), so all 16 `*_trophy` items in `misc.json` are a genuine missing-design gap, not a placement gap. 13 of the 41 converted `misc.json` items do have a real live route: `crabpod_headphones` via the placed `Crab` NPC's music-reward loan/reclaim mechanic (`Crab.playMusic`); `jack_the_pumpkin`/`lem_the_pumpkin`/`shiny_hello_pumpkin`/`steve_the_pumpkin`/`zille_the_pumpkin` via a real, genuinely-enforced-seasonal (Zilloween, via the in-game calendar in `coUserver/lib/endpoints/time.dart`) `knife_and_board` recipe from raw `pumpkin`, plus their `jack_o_lantern`/`lem_o_lantern`/`steve_o_lantern`/`zille_o_lantern` `illuminatePumpkin`-derived lanterns (`hello_pumpkin` itself, notably, has no recipe route and stays unreachable); `new_player_pack_chicken`/`new_player_pack_piggy` via a real `tinkertool` recipe (tinkertool confirmed live `ToolVendor`/`StreetSpirit` stock; the `chicken_egg`/`piggy_egg` ingredients' own upstream source was not independently re-traced this pass); `piggy_plop` via the placed `Piggy` NPC's feed-response ground drop, which also feeds the real `Q9` "Doody Inspector" quest. Item registry now has 193/788 (24.5%) items with real converted icon/sprite art, directly re-verified by grepping every `iconUrl` field across `coUserver/lib/entities/items/json/*.json` after this batch landed -- `keys.json` and `misc.json` are the only two categories now at (effectively) full coverage (12/12 and 41/42; the one gap has no source asset to convert). TENTH batch (2026-08-28, scoped to exactly `food.json` -- the second-largest remaining dead-link category, 142 of 147 items) -- converted all 142 remaining `food.json` items with matching source SWFs under `tmp/glitch-items/food/` (a handful behind an id/folder-name mismatch alias, same family as `cosmapolitan`/`cosma_politan`: `king_of_condiments`->`kings_of_condiments`, `legumes_abbassidienne`->`legumes_parisienne`, `onion_ring`->`onion_rings`, `pineapple_upside_down_pizza`->`papl_upside_down_pizza`, `roasted_pepitas`->`pepitas`, `snocone_*`->`sno_cone_*`, `stinky_cheese`/`very_stinky_cheese`/`very_very_stinky_cheese`->`cheese_stinky`/`cheese_very_stinky`/`cheese_very_very_stinky`); every one of the 142 had a matching source SWF, so no sub-group was left unconverted for lack of source material. Used `ffdec -ignorebackground -export frame` directly (the pipeline doc's confirmed simpler equivalent to the `swf-patch-bgcolor.py`/`swf-export-frame-alpha.py` difference-matting tool) rather than the matting tool, since this batch's sources are the same main-timeline `DefineSceneAndFrameLabelData` family already proven for `-ignorebackground` in the EIGHTH/NINTH batches; re-verified PIL alpha extrema genuinely non-opaque (not `(255,255)`) on every one of the 284 output PNGs (142 items x icon+sprite) before finalizing, 0 failures. Frame counts varied per item (2-5 total main-timeline frames, not uniformly 5 like the spice/drink batches) -- handled generically by using frame 1 as the icon and packing all remaining frames into the sprite strip, rather than assuming a fixed 4-frame stack; `butterfly_butter`/`stinky_cheese`/`very_stinky_cheese`/`very_very_stinky_cheese` (3 frames) and `fried_egg` (4 frames) are the sources with non-5 frame counts confirmed this way, alongside ~26 genuinely 2-frame (icon + single reused stack pose) items. Accessibility, checked individually against real recipe JSON/vendor/Dart-action data rather than assumed from category: 95 of the 142 are real, unmodified recipe outputs from one of `frying_pan`/`saucepan`/`awesome_pot`/`knife_and_board`/`famous_pugilist_grill`/`blender`/`fruit_changing_machine` (all `kitchen`-category tools); the `kitchen` `StreetSpirit` vendor category (109/865 streets in `vendors.json`) is genuinely live -- `StreetSpiritFirebog`/`StreetSpiritZutto`/`StreetSpiritGroddle` are placed by `tools/seed-demo-world.mjs`'s vendor/harvest NPC batch and each reads its own street's real `vendors.json` category at spawn time, confirmed by reading `vendor.dart`'s constructor directly, so a `kitchen`-category spawn genuinely stocks these tools today (same live chain as the FOURTH batch's `blender`/`cocktail_shaker`/`saucepan` finding) -- though this pass did not individually re-trace each of these 95 items' own raw ingredients end-to-end (scope limit, same as the NINTH batch's `chicken_egg`/`piggy_egg` note). 6 more (`mushroom`, `oats`, `oily_dressing`, `olive_oil`, `sesame_oil`, `birch_syrup`) are real, unmodified `groceries`-category `StreetSpirit` stock, same live placement chain. 5 (`butterfly_butter`, `cheese`, `stinky_cheese`, `very_stinky_cheese`, `very_very_stinky_cheese`) form a real, unmodified in-inventory action chain -- `MilkButterCheese.shakeBottle`/`compress`/`age` (`coUserver/lib/entities/items/actions/itemgroups/milk-butter-cheese.dart`) -- that needs no world placement at all since it operates on items already held, not a world entity. `roasted_pepitas` is a real, unmodified `Pumpkins.smash` reward (`coUserver/lib/entities/items/actions/itemgroups/pumpkins.dart`) from smashing any carved pumpkin item (already-converted `misc.json` items from the NINTH batch), chaining back to the already-placed `Garden`'s `pumpkin` crop. 5 (`snocone_blue`/`green`/`orange`/`purple`/`red`) are real, unmodified `SnoConeVendingMachine` vendor stock, but that vendor class is confirmed NOT in `tools/seed-demo-world.mjs`'s `VENDOR_NPC_TYPES` -- real but unplaced, a candidate for a future placement pass alongside `Still`/`HoochRespawningItem` before their own fix. The remaining 30 (`death_to_veg`, `desssert_rub`, `glitchepoix`, `green`, `heston_mash`, `hot_potatoes`, `hototot_rub`, `hungry_nachos`, `kind_breakfurst_burrito`, `king_of_condiments`, `legumes_abbassidienne`, `luxury_tortellini`, `maburger_royale`, `naraka_flame_rub`, `onion_ring`, `pad_tii`, `pi`, `potcorn`, `potians_feast`, `pottine`, `precious_potato_salad`, `red`, `roux`, `salmon_jaella`, `stock_sauce`, `swank_zucchini_loaf`, `swing_batter`, `trump_rub`, `urfu`, `vegmageddon`) were checked against every recipe file (as both an `output` field and a substring match), every vendor `itemsForSale` list/category, and every NPC harvest/reward method under `coUserver/lib/entities/npcs/`, and have **no acquisition route of any kind** -- their only other reference anywhere in `coUserver` is the generic `actions/consume.json` Eat energy/mood table every food item has, not an acquisition route; same genuinely-missing-design category as `bacon`/`potion_rainbow_juice`/`your_papers` from prior batches. `food.json` itself is now at 147/147 (100%) real converted art, 0 remaining dead `childrenofur.com` links. Item registry now has 335/788 (42.5%) items with real converted icon/sprite art, directly re-verified by grepping every `iconUrl` field across `coUserver/lib/entities/items/json/*.json` after this batch landed (433 items still point at dead `childrenofur.com` links, 20 have an empty `iconUrl`); 193(baseline) + 142(this batch) = 335 reconciles exactly, meaning no other agents' conversions landed in `coUserver/lib/entities/items/json/*.json` during this pass. ELEVENTH batch (2026-08-28, scoped to exactly `toys.json`/`tools.json`/`alchemical.json`) — converted all 115 remaining dead-linked items across the 3 files (48/50 toys.json, 38/43 tools.json, 29/32 alchemical.json — the other 2/50 and 5/43 in those files were already real from earlier batches), every one with a matching source SWF (0 sub-groups had to be skipped for missing source material). Wrote `tools/swf-scene-labels.py` (new tool, parses the main-timeline `DefineSceneAndFrameLabelData` tag directly, distinct from `swf-frame-labels.py`'s per-`DefineSprite` `FrameLabel` handling) to resolve real frame numbers for named states (`iconic`, `1`..`N`/`5`..`1` quantity-stack poses, `tool_animation`, `broken`/`broken_iconic`) instead of guessing; cross-checked every icon/sprite frame choice against which named frame's original dead-URL filename suffix matched the item's actual `iconUrl`/`spriteUrl` (e.g. `class_axe`'s `iconUrl`==`spriteUrl` both pointed at the `_iconic_` file, so both outputs use the `iconic`-labeled frame, not the `1`-labeled one most sibling tools use). Used `ffdec -ignorebackground -export frame` (no matting needed, genuine alpha confirmed on all 115×2=230 output PNGs via PIL `getextrema()`, 0 degenerate). `toolAnimation`/`brokenUrl` fields left unconverted (out of iconUrl/spriteUrl-only scope, matching the `item-hoe` precedent). Accessibility, checked individually against `ToolVendor.itemsForSale`/every `StreetSpirit` vendorType case/every `recipes/json/*.json` output rather than assumed: 27/48 toys are real live `toy`-category `StreetSpirit` stock (placed); 21/48 toys (10 race tickets + `ticket_gameshow`, 2 dolls not `ayn_rand_doll`'s siblings, `fortune`, `fortune_cookie_withfortune`, `gift_box_wrapped`, 2 glitchmas-cracker contents, `play_cube`, `random_kindness`) have **no acquisition route of any kind** — several (`fortune_cookie_withfortune` via 'Insert Note', the 2 cracker contents via an 'open' action) are specifically blocked by the same "actionName is pure item-json metadata with no server-side handler" pattern documented for achievements in the NINTH batch, not merely unplaced. 31/38 tools are real live `ToolVendor`/`StreetSpirit`-category stock (22 direct `ToolVendor.itemsForSale` entries + 9 `StreetSpirit`-only kitchen/mining/alchemical categories) and 3 more (`high_class_hoe`, `irrigator_9000`, `super_scraper`) are real, live, multi-hop `tinkertool` recipe outputs (traced through `alchemical_tongs`/`smelter`/`hatchet` to already-placed rock/tree harvests, same chain as the SIXTH batch's `advanced_resources.json` finding); the remaining 4 (`ace_of_spades`, `class_axe`, `splendid_spindle`, `fox_brush`) have **no acquisition route** — each is referenced only as an interchangeable-tool option (`ace_of_spades`~`shovel`, `class_axe`~`hatchet`) or a hard requirement (`fox_brush` for `Fox`'s brushing action) in Dart item-requirement checks, never as a vendor/recipe output. All 29 remaining `alchemical.json` items are real, live, multi-hop recipe outputs — 15 via `test_tube` (from red/green/blue/shiny elements), 9 via `beaker` (from `test_tube` compounds), 3 (`green_element`/`red_element`/`shiny_element`) via `grinder` (same rock/fruit-harvest chain as the already-placed `blue_element`) — except `old_tree_poison`/`old_tree_poison_antidote`, which have **no acquisition route of any kind** (grepped the whole `coUserver`/`authServer` tree, found nowhere but their own item definition). `toys.json`/`tools.json`/`alchemical.json` are now at 100% real converted art (0 remaining dead `childrenofur.com` links in any of the 3 files). Item registry now has 745/788 (94.5%) items with real converted icon/sprite art, directly re-verified by grepping every `iconUrl` field across `coUserver/lib/entities/items/json/*.json` after this batch landed (23 items still point at dead `childrenofur.com` links — scattered across `basic_resources.json`/`collectibles.json`/`misc.json`/`quest_items.json`/`storage.json`, none in this batch's 3 files — plus 20 with an empty/non-standard `iconUrl`, e.g. `collectibles.json`'s 19 no-source-SWF items); 335(TENTH-batch baseline) + 115(this batch) = 450, vs. 745 actual, meaning +295 items landed from other agents' concurrent conversions of other categories during this pass, per the parallel-work note at the top of this task. TWELFTH batch (2026-08-28, scoped to exactly `collectibles.json` -- the largest remaining unconverted category at the time, 190 of 212 items) -- converted 171 of the 190 dead-link items (all with a matching source SWF, found by matching each item's dead `childrenofur.com` iconUrl filename stem against every `.swf` basename under `tmp/glitch-items`, catching 2 filename-typo cases the naive match missed: `artifact_butterlfy_hair_clip_piece{1-4}.swf` for the `piece_of_butterfly_hair_clip_*` items, and `egghunt_egg_1.swf`'s non-standard iconUrl format for `certified_organic_chocolate_egg`). Two conversion sub-modes: (1) 44 `cubimal_*` items (`npc_cubimal_*.swf`) -- real art lives in a nested `DefineSprite` (char id 14, `cubimal_mc_1`) rather than the main timeline, so `ffdec -export sprite` was used directly (genuine alpha with no matting needed, confirmed by PIL); frame 1 (labeled `"1"`, the default/idle pose) used for both icon and sprite, leaving frame 2 (labeled `"race"`, used live by the pre-existing `RacingCubimal` entity) out of scope. (2) 127 main-timeline items (artifact families + their broken-piece fragments, trophy-piece fragments, musicblocks, gems, petrified rocks, Egg Hunt eggs, and assorted rare/novelty items) -- used `tools/swf-export-frame-alpha.py` (difference-matting) per prior-batch convention; frame 1 = icon, frame 2 = sprite when a 2nd frame exists, else the sole frame reused for both (same convention as `wooden_apple`/`magical_pendant`). All 342 output PNGs (171 items x icon+sprite) re-verified via PIL alpha extrema genuinely non-opaque (not `(255,255)`) before finalizing, 0 failures across the whole batch. Accessibility, checked individually against real Dart/JSON game logic rather than assumed: a major, previously-undocumented finding -- 43 of the 44 cubimals (all but `cubimal_factorydefect_chick`, which is not a key in either table) are real, unmodified, **live end-to-end** via `CubimalBox.takeOutCubimal` (`coUserver/lib/entities/items/actions/itemgroups/cubimals.dart`): opening a `cubimal_series_1_box` or `cubimal_series_2_box` (both real, unmodified `toy`-category `ToolVendor`/`StreetSpirit` stock, both vendor classes already placed by the 2026-08-28 vendor/harvest NPC placement pass) grants a random named cubimal item straight to inventory via `InventoryV2.addItemToUser`, keyed by the exact `cubimal_<name>` item id used here. All other 127 converted items, and `cubimal_factorydefect_chick`, were checked against every vendor `itemsForSale` list/category and every `recipes/json/*.json` file directly and found to have **no acquisition route of any kind** -- the artifact families (necklaces/beads, hair clip, chicken brick, mysterious cube, mirror with scribbles, nose of china, platinumium spork, torn manuscript, glove with metal finger) and their broken-piece fragments have no "assemble pieces into whole item" action anywhere in `coUserver` (same finding the SEVENTH batch already made for `wooden_apple`/`magical_pendant`, now confirmed to extend to every sibling artifact family); the 20 `piece_of_street_creator_*_trophy_*` fragments likewise have no combiner code, even though `misc.json`'s already-converted whole `street_creator_*_trophy` items narratively describe collecting "all five parts" of exactly these pieces; musicblocks are a real placed *sink* (`Crab.playMusic` consumes them from a player's `musicblock_bag` Crabpack) but have no matching *source* anywhere; gems/petrified rocks/Egg Hunt eggs/Glitchmas-promo/beta-tester-novelty items were originally acquired via real-money purchase, seasonal event, or promo code that was never ported into `coUserver`'s game logic at all. Confirmed a genuine, source-verified dead end for a future pass to skip re-attempting: 19 `collectibles.json` items (`button_shape_of_{bird,cloud,frog,wasp}`, `caterpillar_trousers`, `ear_trumpet`, `extremely_long_scarf`, `fake_nose_made_of_{plaster,wool}`, `fan`, `handbag_clasp`, `intricately_carved_wooden_ear`, `oversized_protective_goggles`, `pouch_full_of_old_seeds`, `remains_of_a_daybag`, `tangled_pair_of_sock_garters`, `three_cornered_hat`, `twenty_nine_cornered_hat`, `very_tall_top_hat`) are a coherent "wearable artifact accessory" sub-family with dead iconUrls but **no matching source SWF anywhere** under `tmp/glitch-items` (the `artifacts/` folder was listed in full and contains exactly 11 artifact families, none of these); together with the pre-existing 20 items whose `iconUrl` was already empty in the source data (no filename to even attempt a match against, also individually re-checked by item name this pass with no hits), `collectibles.json` is now at its ceiling of 173/212 (81.6%) real converted art given currently-vendored source material -- the remaining 39 are not oversights. Item registry now has 745/788 (94.5%) items with real converted icon/sprite art, directly re-verified by grepping every `iconUrl` field across `coUserver/lib/entities/items/json/*.json` after this batch landed (unchanged from the figure already recorded in the row above, confirming no other agent's `collectibles.json` edits raced this batch's write). THIRTEENTH batch (2026-08-28, orchestrated as 7 parallel sub-agents, scoped to exactly `tinctures-potions.json`/`croppery-gardening.json`/`emblems-icons.json`/`storage.json`/`herdkeeping.json`+`quest_items.json`+`basic_resources.json`+`misc.json`/`drinks.json`/`advanced_resources.json`+`machines-fuel.json`, to avoid colliding with other agents converting other categories concurrently in the same session) -- converted the entire remaining dead-linked backlog in those 11 files: 124 items (22 tinctures-potions, 20 croppery-gardening, 19 emblems-icons, 15 storage, 13 herdkeeping, 3 quest_items, 13 drinks, 10 advanced_resources, 9 machines-fuel). All used `ffdec -ignorebackground -export frame`/`-export sprite` (genuine PIL alpha `(0,255)` on every output, no opaque-background defect hit, the difference-matting fallback was never needed). New frame-pattern families beyond the established 5-frame icon+stack-strip convention: a 77-frame potion family (`iconic`/`1`/`tool_animation`, frame 1 reused for both outputs); a 2-frame essence family (`iconic`/`1`); 1/2/3-frame storage-bag families (some with a genuinely distinct 2nd/3rd static pose, not a stack strip); a 2-frame emblem/icon family; a 2-frame `beam`/`board`/`girder`/`wood_post` family with two genuinely distinct static poses (angled-held vs. flat-lying, not an animation); several 1- or near-identical-2-frame machine-component statics (matching the `machine_stand` precedent); and `fox_bait`'s real art in a nested `DefineSprite` with labeled `stink1`/`stink2`/`stink3` decay frames (a strong candidate to resolve the separately-tracked world-entity `FoxBait` sprite gap in `fox.dart`, not attempted here). Accessibility, checked individually per item against real Dart/JSON game logic: the 9 emblems are live via the generic giant-favor-threshold `Shrine.donate()` mechanism (all 11 giant Shrines placed); the 10 icons are live via the generic `Emblem.iconize()` action (11 emblems -> 1 icon); the 13 crop seeds are live via `Garden.ITEM_REQ_PLANT` + the live `gardening` `StreetSpirit` category; the 7 tree-bean items are live `bean_seasoner` recipe outputs (not Tree-class planting inputs, confirmed by direct search); all 15 converted `storage.json` items are live via the generic `Vendor.pickItems(["Storage"])` category mechanism; 12 of 13 `herdkeeping.json` items and `small_worthless` are live (vendor stock, `tinkertool`/`egg_seasoner` recipes, or `MilkButterCheese.prod()`), while `fox_bait`, `hogtied_piggy`, `juju_trowel`, and `note_hint` have **no acquisition route of any kind**; 12 of 13 `drinks.json` items trace end-to-end to already-live sources (cocktail_shaker/blender recipe chains, or `IceNubbin.collect()`'s melt-byproduct for `cup_of_water`), while `wine_of_the_dead` has **no route** (its only candidate, `HellBartender.glassOfWine()`, is a stub that grants nothing even if placed); all 10 `advanced_resources.json` items in this batch have **no acquisition route of any kind** (re-verified against all 22 recipe files and every vendor stock mechanism); all 9 `machines-fuel.json` items are live, hardcoded `ToolVendor.itemsForSale` stock. 4 items had no matching source SWF after a targeted re-search each -- `musicblock_bag`/Crabpack, `user_made_quest`, `salmon_bubble`, `new_player_pack_butterfly` -- all recorded in `content/runtime-manifest.json`'s `deliberatelyDeferred` with an honest reason; notably all 4 DO have real, live, non-art acquisition routes despite the missing art (`Crab.buyCrabpack()`, `QuestService.createQuestItem()`, `Salmon.pocket()`'s miss branch, and the `tinkertool` new-player-pack recipe respectively). All 11 assigned files are now at 100% real converted art except the 4 no-source items. Item registry now has 745/788 (94.5%) items with real converted icon/sprite art, directly re-verified by grepping every `iconUrl` field across `coUserver/lib/entities/items/json/*.json` after this batch landed (unchanged from the figure already recorded above, confirming this batch's manifest merge -- done via a fresh re-read of `content/source-manifest.json`/`content/runtime-manifest.json` immediately before writing, per the session's concurrency protocol -- did not race any other agent's concurrent write). `tools/validate-content.mjs` passes (1788 sprite sheets checked, 785 runtime-manifest assets, 13 deliberately-deferred entries, 126 placement rows). FOURTEENTH batch (2026-08-28, world-entity closing pass) — converted the last 9 world-entity classes' own on-map art (distinct from item-icon art, which was already at 745/788 before this batch and is unaffected by it): `DustTrap` (`coUserver/lib/entities/npcs/dust_trap.dart`, 4 states from `tmp/glitch-items/misc/dust_trap/dust_trap.swf`), `Mailbox` (`coUserver/lib/entities/npcs/mailbox.dart`, 5 states from `tmp/glitch-items/misc/npc/npc_mailbox/npc_mailbox.swf`), `GardeningGoodsVendor` (`coUserver/lib/entities/npcs/vendors/scarecrow.dart` — the file is named `scarecrow.dart` but the actual class is `GardeningGoodsVendor`; confirmed by grep that no `class Scarecrow` exists anywhere in `coUserver` — 5 states from `tmp/glitch-items/inhabitants/gardening_vendor/npc_gardening_vendor.swf`, also fixing 2 of the 5 states' pre-existing dead frame-count metadata (`walk`/`walk_end`) which did not match the real source SWF, same bug class as the earlier `woodtree.dart` fix), 4 `RespawningItem` growth-stage world entities (`CocktailShakerRespawningItem`, `HotNFizzySauceRespawningItem`, `LaughingGasRespawningItem`, `NoNoPowderRespawningItem`, each 1 state from their own source SWF, same `hooch`/`purple_flower`/`awesome_stew`-family convention), `Icon` (`coUserver/lib/entities/npcs/items/icon.dart`, all 11 giant skins from `tmp/glitch-items/giants/icon_<name>/icon_<name>.swf`), and `RacingCubimal` (`coUserver/lib/entities/npcs/items/cubimal.dart`, all 44 skins from `tmp/glitch-items/cubimals/npc_cubimal_<name>/` or, for `factorydefect_chick`, `tmp/glitch-items/rare_items/npc_cubimal_factorydefect_chick/`, scripted via a new `tmp/final9_frames/batch_cubimal.py` helper for reproducibility across all 44 files). All used `ffdec -ignorebackground -export sprite`/`-export frame` (no opaque-background defect on any of these 62 source SWFs; PIL alpha `getextrema()` genuinely `(0,255)` on every one of the ~90 output PNGs, verified before finalizing). Placement/accessibility, checked individually rather than assumed: `DustTrap`/`Mailbox`/`GardeningGoodsVendor`/the 4 `RespawningItem` classes are NOT in `tools/seed-demo-world.mjs` and NOT in `entity_item.dart`'s `ITEM_ENTITIES` map, so each has real, unmodified, but currently-unreachable game logic — same real-but-unplaced pattern as the earlier `awesome_stew`/`butterfly_milk`/etc. batch, honestly documented as NOT yet live pending a future placement pass (the 4 `RespawningItem` classes' matching player items are separately, already obtainable via vendor/recipe routes documented in earlier batches, so only the ground-spawn harvest route itself is blocked). `Icon` and `RacingCubimal`, by contrast, turned out to be genuinely live end-to-end today with NO placement-pass needed at all — a distinct and better accessibility shape than assumed at batch start: both are dynamically spawned by a real player item action (`icon_of_<giant>`'s `Place` action -> `Item.place` -> `EntityItem.place` -> `ITEM_ENTITIES` lookup -> `StreetEntities.setEntity`; `cubimal_<name>`'s `Race` action -> `Cubimal.race()` in `cubimals.dart`, which reads the same `ITEM_ENTITIES` map directly), confirmed by reading `item.dart`/`entity_item.dart`/`cubimals.dart` directly rather than assumed from the class's presence in `ITEM_ENTITIES`. Both triggering items are themselves already live (`icon_of_<giant>` via `Emblem.iconize()` from 11 placed-Shrine-sourced emblems; `cubimal_<name>` via `CubimalBox.takeOutCubimal` from vendor-stocked `cubimal_series_1_box`/`cubimal_series_2_box`, 43 of 44 skins — `factorydefect_chick` has art but no box-drop-table entry, a pre-existing gap documented in the TWELFTH batch). `content/source-manifest.json` gained 62 new entries (one per converted source SWF: 3 multi-state NPCs + 4 single-state respawning items + 11 icon skins + 44 cubimal skins) and `content/runtime-manifest.json` gained 9 new entries (one per entity class, matching this task's own framing). This closes out every world-entity class this recovery project's asset-conversion passes identified as having real game logic but dead/missing on-map art — no further "N world-entity classes remain fully unconverted" backlog is known at this time (a future placement pass for the 3 NPCs + 4 respawning items, and a source-SWF search for the still-undocumented `FoxBait`/animSpriteUrl-family gaps noted in earlier batches, remain open separately). `tools/validate-content.mjs` passed at that point (1861 sprite sheets checked, 794 runtime-manifest assets, 13 deliberately-deferred entries, 129 placement rows). PLACEMENT PASS (2026-08-28, same day): all 7 of the above real-but-unplaced classes (`DustTrap`/`Mailbox`/`GardeningGoodsVendor`/`CocktailShakerRespawningItem`/`HotNFizzySauceRespawningItem`/`LaughingGasRespawningItem`/`NoNoPowderRespawningItem`) added to `tools/seed-demo-world.mjs`'s `REAL_TYPES` (via `VENDOR_NPC_TYPES`/`NPC_ITEM_TYPES`/`RESPAWNING_ITEM_TYPES`), each superclass chain confirmed directly against `street.dart`'s reflective `putEntitiesInMemory` rather than assumed: `GardeningGoodsVendor extends Vendor` (Vendor-shaped construction args); `DustTrap extends NPC` but is one of `putEntitiesInMemory`'s two special-cased types that also get Vendor-shaped args (`classMirror == findClassMirror("DustTrap")`); `Mailbox extends NPC` (plain NPC-shaped args, like `Still`/`Crab`); all 4 `RespawningItem` subclasses `extend RespawningItem extends Plant` (plain Plant-shaped args). Rarity: `DustTrap`/`Mailbox` 6% (Still's tier, "structure" NPCs); `GardeningGoodsVendor` 11% base, boosted to ~18% on `vendors.json` "gardening"-category streets (its stock -- hoe/watering_can/13 seeds -- is exactly that category's own definition, sharing `Garden`'s existing boost logic but not its produce boost, since it stocks no produce); the 4 respawning items tiered by their own in-code `respawnTime` (the same grounded signal the earlier Hooch/PurpleFlower placement used) -- `NoNoPowderRespawningItem` 35% (30s respawn, fastest in the game), `LaughingGasRespawningItem` 30% (3min, PurpleFlower's own respawnTime), `HotNFizzySauceRespawningItem` 25% (6min, near Hooch's 7min), `CocktailShakerRespawningItem` 12% (45min, 6-15x slower than the other three, and rewards a tool rather than a consumable). Seeded live (64,429 rows total; 174/193/379/391/770/1304/1383 rows respectively for the 7 new types) and `game-server` restarted cleanly. Force-loading a street for each of the 7 via a `streetUpdate` WebSocket probe surfaced one genuine pre-existing bug: `GardeningGoodsVendor.SELL_ITEMS` (`scarecrow.dart`) was declared `List<Map<String, dynamic>>`, but `Item.getMap()` (`item.dart`) returns untyped `Map` (inferred `Map<dynamic, dynamic>`) -- the stricter generic threw "type '_InternalLinkedHashMap<dynamic, dynamic>' is not a subtype of type 'Map<String, dynamic>'" the moment the class was ever actually constructed (never triggered before since it was never placed), which also silently aborted the rest of that street's entity-load loop (`putEntitiesInMemory`'s catch does `return false` for the whole street, not just the failing entity) -- fixed by loosening the field to `List<Map>` (matching `Vendor`'s own untyped `itemsForSale` field), a type-annotation-only change with no behavior difference. Re-probed after the fix: all 7 types now construct and their streets load/unload cleanly with no "Unable to instantiate" warning; `DustTrap`/`Mailbox` in particular (the two using the non-standard Vendor-shaped/special-cased constructor-argument path) were explicitly re-checked and confirmed clean. `content/runtime-manifest.json` updated for all 7 (`destination` already "World entity"; `accessibleThrough` rewritten to record the live placement, rarity, and verification). `tools/validate-content.mjs` re-run and still passes post-placement (same 1861/794/13 counts; 150 placement-manifest example rows, up from 129, covering 59 entity types). |
| planned | Pre-existing crash: street with no CAT422 geometry | Found (not caused) while placing the second entity batch: loading a street lacking CAT422 location data throws `NoSuchMethodError: The method '[]' was called on null` in `street.dart` (indexing `dynamic['ground_y']` on a null map). Unrelated to any placed entity — affects any player visiting such a street. Not yet fixed. |
| planned | Pre-existing crash: `List<dynamic>` cast in quest favor rewards | Found (not caused) while restarting `game-server` for the hooch/purple_flower/crabato_juice placement pass: the container was already down, killed by `type 'List<dynamic>' is not a subtype of type 'List<QuestFavor>'` in `MetabolicsChange.trySetMetabolics` (`coUserver/lib/endpoints/metabolics/metabolics.dart:48`), reached via `Quest._giveRewards` <- `Quest.handleEvent` <- `Tree.pet`'s reward chain (`coUserver/lib/quests/quest.dart`, `coUserver/lib/entities/plants/trees/tree.dart:225`). Same family of bug as the "NPC/item interaction crash chain" row above (an untyped/raw decoded value assigned into a typed field) but a not-yet-fixed instance of it, in the quest-favor-reward path specifically. Unrelated to any entity placed in this pass. Restarted cleanly and did not recur during this pass's own verification, but will recur whenever a quest awards a favor via this path. Not yet fixed. |
| done | Placement pass: hooch/purple_flower/crabato_juice | Found across the fourth/fifth asset-conversion batches (see "Continue asset conversion" above): `HoochRespawningItem`, `PurpleFlowerRespawningItem` (both `coUserver/lib/entities/plants/respawning_items/*.dart`, `RespawningItem` -> `Plant` world-entity harvestables), `Still` (`coUserver/lib/entities/npcs/items/still.dart`, an `EntityItem` -> `NPC`), and `Crab` (`coUserver/lib/entities/npcs/crab.dart`, an `NPC`) all had real, unmodified, working acquisition logic that was simply never placed. Verified directly (not trusted from the prior batch notes alone) by reading all four classes: each uses the exact `(id, x, y, z, rotation, h_flip, streetName)` constructor `street.dart`'s reflective `putEntitiesInMemory` expects, and all four source files are already `part of entity;` in `entity.dart` -- no server-side Dart changes were needed, only `tools/seed-demo-world.mjs`. Placed 2026-08-28: `HoochRespawningItem` (~25% chance/street, maxCount 1), `PurpleFlowerRespawningItem` (~35%, maxCount 2), `Still` (~6%, maxCount 1, same low "structure NPC" tier as `ToolVendor`), `Crab` (~22%, maxCount 1, same tier as the other wildlife-ish creature NPCs). No `REGION_THEME` entries added for any of the four: no confident biome association was found in recovered data for any of them, same "don't guess" rule already applied to the shrines/`StreetSpiritZutto`/`Groddle`. Verified live: seeded ~61.6k rows (up from ~58.5k) to the running Postgres DB and restarted `game-server` (clean startup, zero errors); a WebSocket probe (connecting directly to the `streetUpdate` handler at `ws://localhost:8282/streetUpdate`, which force-loads any street whose tsid isn't yet in memory with no auth required) force-loaded one street per new type -- all four constructed and later unloaded cleanly with zero related exceptions in `docker compose logs game-server`. Honest caveat: `Crab`'s world-entity sprite was already converted in an earlier NPC batch and renders correctly, but `HoochRespawningItem`/`PurpleFlowerRespawningItem`/`Still` still point their own on-map `Spritesheet` at dead `childrenofur.com` URLs (only their *item-icon* art was converted, not their world-entity sprite) -- their gameplay logic (harvest/ferment/reward) is now genuinely reachable, but the in-world sprite for those three will render broken until a future art-conversion pass. `content/runtime-manifest.json`'s `crab`/`item-hooch`/`item-crabato_juice`/`item-purple_flower` rows and `content/placement-manifest.json` updated to reflect placement; `tools/validate-content.mjs` still passes. Separately (pre-existing, unrelated to this placement): the `game-server` container was found already crashed on startup with `type 'List<dynamic>' is not a subtype of type 'List<QuestFavor>'` in `MetabolicsChange.trySetMetabolics` (via `Quest._giveRewards` <- `Tree.pet`'s reward chain) -- restarted cleanly before this pass's own verification; not fixed here, out of scope. |
| done | World-entity art: hooch/purple_flower/still finish, plus 9 new RespawningItem conversions | Scoped to WORLD-ENTITY art only (not item icons), per the parallel-work split with other agents converting item-icon categories. Two parts. (1) Finished the 3 classes RECOVERY_TODO's "Placement pass: hooch/purple_flower/crabato_juice" row flagged as placed-but-still-broken: `HoochRespawningItem`, `PurpleFlowerRespawningItem` (`coUserver/lib/entities/plants/respawning_items/{hooch,purple_flower}.dart`), and `Still` (`coUserver/lib/entities/npcs/items/still.dart`) all had real converted *item*-icon art but their own on-map `Spritesheet` still pointed at dead `childrenofur.com` URLs. Converted from the same source SWFs already used for their item icons (`tmp/glitch-items/drink/hooch/hooch.swf`, `tmp/glitch-items/herbs/purple_flower/purple_flower.swf`) — reusing frames 2-5 (the growth-stage poses, distinct from frame 1 "iconic" already used for the inventory icon) for hooch/purple_flower's single `'1-2-3-4'` state; `Still` (`tmp/glitch-items/misc/still/still.swf`) required resolving 4 real `FrameLabel` tags (`empty`/`active`/`ready`/`collect`) inside a nested `DefineSprite` (chid 120) via `tools/swf-frame-labels.py`, whose resolved frame counts (1/99/41/119) exactly matched the pre-existing dead-link Spritesheet's own declared `numFrames`, confirming correct resolution. All 3 are already placed in `tools/seed-demo-world.mjs` (not touched this pass) — they now both spawn and render correctly, closing the "still point at dead URLs" caveat from the placement-pass row above. (2) Converted 9 more `RespawningItem` world-entity classes with real matching source SWFs but no prior conversion attempt at all: `awesome_stew`, `butterfly_milk`, `cinnamon`, `coffee`, `earthshaker`, `fruity_juice`, `hell_grapes` (class `HellGrapes`, itemType `bunch_of_grapes`), `hell_tomato` (class `HellTomato`, itemType `tomato`), `plain_bubble` (all `coUserver/lib/entities/plants/respawning_items/*.dart`). Each inherits `RespawningItem`'s real, unmodified `pick up` action (`InventoryV2.addItemToUser` on a real item id); `HellGrapes` additionally has its own real `squish` action for a metabolics/stat reward. **None of these 9 are placed in `tools/seed-demo-world.mjs`** (checked directly by grep before and after this pass; `tools/seed-demo-world.mjs` itself was not modified) — same real-but-unplaced gap already documented and resolved for hooch/purple_flower/still/crab, now recorded honestly as a placement candidate for a future pass, not claimed as live. CRITICAL defect found and fixed during this batch: a first `ffdec -export frame` attempt on all 12 source SWFs produced frames with a fully (or near-fully, alpha ≥ 254) opaque baked-in white background matte instead of real transparency (PIL `Image.open(...).split()[-1].getextrema()` returned `(255,255)`/`(254,255)` on 11 of 12 outputs) — the exact ffdec export defect this pipeline's docs warn about. Fixed by re-exporting with ffdec's `-ignorebackground` flag; re-verified genuine 0-255 alpha extrema and fully transparent `(0,0,0,0)` sheet corners on every one of the 15 output files (11 single-strip + 4 for Still) before wiring any Spritesheet URL. Frame geometry (packed sheet `frameWidth × numFrames = sheetWidth`, `frameHeight = sheetHeight`) verified to exactly match the updated `Spritesheet()` constructor args in every edited `.dart` file. `content/source-manifest.json` (12 new entries: `hooch`/`purple_flower`/`still`/`awesome_stew`/`butterfly_milk`/`cinnamon`/`coffee`/`earthshaker`/`fruity_juice`/`hell_grapes`/`hell_tomato`/`plain_bubble`) and `content/runtime-manifest.json` (same 12 new "World entity" rows, plus corrected `item-hooch`/`item-purple_flower` statusNotes to reflect their world-entity sprite no longer being broken) updated; `tools/validate-content.mjs` passes. `StreetSpiritGroddle`'s remaining 23 giant/time-of-day variant combinations were investigated and confirmed still out of scope — `content/runtime-manifest.json`'s pre-existing `street_spirit_groddle_reskins` deferral entry already documents why (the combinations are assembled at runtime from layered part-symbols via AS3 code, not pre-rendered timelines ffdec can export directly; needs new multi-layer compositing tooling, not attempted here). |
| done | Vendor/harvest NPC family placement | Three real, unmodified NPC routes (`Garden`'s 13-crop `CROPS`/`harvest()`, `ToolVendor.itemsForSale`, and `StreetSpirit`'s per-street-category vendor stock via `vendors.json`) had real converted art but were never spawned anywhere — see POTENTIAL_TODO.md's "Item economy" section, which found this gap across three separate conversion batches. Placed 2026-08-28 by extending `tools/seed-demo-world.mjs`: `Garden` (~20% chance/street, boosted on vendors.json `gardening`/`produce` streets), `ToolVendor` (~12%), and the 3 concrete real-art `StreetSpirit` subclasses — `StreetSpiritFirebog` (~4% baseline, ~36% in the firebog region, its documented home biome), `StreetSpiritZutto` (~12% flat, no region guess), `StreetSpiritGroddle` (~15% flat, no region guess). Deliberately NOT placed: the abstract `StreetSpirit` base class itself — it has no `states`/`setState()` call, so its inherited `update()` would dereference a null `currentState` and crash on the first simulate tick (confirmed by reading `street_spirit.dart`); its 3 concrete subclasses are used instead, and each already reads `vendors.json`'s per-street category automatically via the pre-existing `Vendor` constructor (no new category-assignment logic was written). Verified live: after seeding ~58k rows to the running Postgres DB and restarting `game-server` (0 restarts, no crash), a WebSocket probe forced 4 target streets (one holding both `Garden`+`ToolVendor`, one each for `StreetSpiritFirebog`/`Zutto`/`Groddle`, including its async giant-detection lookup) to load — all constructed and later unloaded cleanly with zero related exceptions in `docker compose logs game-server`. `content/runtime-manifest.json`'s 5 corresponding rows and `content/placement-manifest.json` updated to reflect placement; `tools/validate-content.mjs` still passes. |
| done | Placement pass: SnoConeVendingMachine (Q7 fix) | Found by POTENTIAL_TODO.md's 2026-08-28 quest completability audit: quest Q7 "Snocone Joy" (`coUserver/lib/quests/json/snocone_joy.json`) requires buying any `snocone_*` item, and `SnoConeVendingMachine` (`coUserver/lib/entities/npcs/vendors/snoconevendingmachine.dart`) is the only source of any `snocone_blue`/`green`/`orange`/`purple`/`red` item anywhere in the codebase, but the class was not in `tools/seed-demo-world.mjs`'s `VENDOR_NPC_TYPES` -- never instantiated anywhere, blocking Q7. Verified directly (not trusted from the audit note alone) before placing: `SnoConeVendingMachine(String id, String streetName, String tsid, num x, num y, num z, num rotation, bool h_flip)` matches exactly the `[id, label, tsid, x, y, z, rotation, h_flip]` argument order `street.dart`'s reflective `putEntitiesInMemory` uses for every `Vendor` subclass, identical in shape to the already-placed `ToolVendor`. Unlike the hooch/purple_flower/still precedent, this NPC's own on-map appearance art is NOT dead-linked: `content/runtime-manifest.json`'s pre-existing `snoconevendingmachine` row already documented its idle/walk/talk/attract `Spritesheet`s as converted (`files/sprites/generated/converted/sno_cone-*.png`, confirmed present on disk and referenced directly by the class), and its catalog's 5 `snocone_*` item icons were separately converted in an earlier item-icon batch (10th batch, `food.json`) -- both the NPC and its wares are genuinely visually complete, only placement was missing. Placed 2026-08-28 in `tools/seed-demo-world.mjs`: `SnoConeVendingMachine` (10% chance/street, maxCount 1 -- between `ToolVendor`'s 12% and `Still`'s 6%, reflecting its narrower fixed 5-item catalog; no `REGION_THEME` entry, since `vendors.json`'s 9 vendor categories include no snocone-related category to theme on). Verified live: seeded 336 live instances (337 generated, 1 lost to a pre-existing, unrelated duplicate-tsid id collision affecting all `demo-`-id types equally, not specific to this change) to the running Postgres DB and restarted `game-server` (clean startup, zero errors); a WebSocket probe (`ws://localhost:8282/streetUpdate`) force-loaded street "17 Adanac" (`LLI161CEI2J10K4`), which holds a newly-placed `SnoConeVendingMachine` -- constructed and later unloaded cleanly with zero related exceptions in `docker compose logs game-server`. `content/runtime-manifest.json`'s `snoconevendingmachine` row and `content/placement-manifest.json` updated to reflect placement; Q7 is now genuinely completable end-to-end (see POTENTIAL_TODO.md's quest audit table, updated from PARTIALLY BLOCKED to FULLY COMPLETABLE). `tools/validate-content.mjs` still passes. |
| done | Placement pass: DustTrap/Mailbox/GardeningGoodsVendor/4 respawning items | Closes out the "real, unmodified game logic, real converted art, never placed" gap left open by the twelfth conversion batch's own "Continue asset conversion" row above for `DustTrap`, `Mailbox`, `GardeningGoodsVendor` (`coUserver/lib/entities/npcs/vendors/scarecrow.dart` -- file misnamed, no `class Scarecrow` exists), and `CocktailShakerRespawningItem`/`HotNFizzySauceRespawningItem`/`LaughingGasRespawningItem`/`NoNoPowderRespawningItem`. Superclass chain confirmed directly against `street.dart`'s reflective `putEntitiesInMemory` for each, not assumed: `GardeningGoodsVendor extends Vendor` (Vendor-shaped `[id, label, tsid, x, y, z, rotation, h_flip]` args); `DustTrap extends NPC` but is one of `putEntitiesInMemory`'s two special-cased types that also get Vendor-shaped args; `Mailbox extends NPC` (plain `[id, x, y, z, rotation, h_flip, streetName]` args, like `Still`/`Crab`); all 4 `RespawningItem` subclasses `extend RespawningItem extends Plant` (same plain-args shape). Added to `tools/seed-demo-world.mjs`'s `REAL_TYPES` with rarity tiered by direct analogy/signal (not guessed): `DustTrap`/`Mailbox` 6% (Still's "structure NPC" tier); `GardeningGoodsVendor` 11% base, boosted to ~18% on `vendors.json` "gardening"-category streets, sharing `Garden`'s existing gardening-boost logic since its stock (hoe/watering_can/13 seeds) is exactly that category's own definition; the 4 respawning items tiered by their own in-code `respawnTime` (30s/3min/6min/45min -> 35%/30%/25%/12%). Seeded live (64,429 rows total; 174-1383 rows per new type) and `game-server` restarted cleanly. Force-loading a street per type via a `streetUpdate` WebSocket probe surfaced one genuine pre-existing bug -- `GardeningGoodsVendor.SELL_ITEMS` was declared `List<Map<String, dynamic>>` but `Item.getMap()` returns untyped `Map`, throwing a runtime type-check error on its first-ever construction and silently aborting the rest of that street's entity-load loop (`putEntitiesInMemory`'s catch returns false for the whole street) -- fixed by loosening the field to `List<Map>` (matching `Vendor`'s own untyped `itemsForSale` field; type-annotation-only, no behavior change). Re-probed after the fix: all 7 types construct and their streets load/unload cleanly with zero "Unable to instantiate" warnings, `DustTrap`/`Mailbox` (the two non-standard constructor-argument paths) explicitly re-checked. `content/runtime-manifest.json` updated for all 7 (`accessibleThrough` rewritten to record live placement/rarity/verification). `tools/validate-content.mjs` still passes (1861 sprite sheets, 794 runtime-manifest assets, 13 deliberately-deferred, 150 placement-manifest example rows covering 59 entity types). |
| done | FLA/SWF -> sprite conversion pipeline | Built and proven (`tools/README-swf-pipeline.md`, `tools/swf-extract-bitmaps.py`, `tools/swf-frame-labels.py`, `tools/build-sprite-sheet.py`, `tools/swf-find-labeled-sprite.py`). Requires the external JPEXS `ffdec` tool (not vendored; downloaded on demand, see README). `content/source-manifest.json` / `content/runtime-manifest.json` / `content/placement-manifest.json` + `tools/validate-content.mjs` enforce geometry, file existence, item-prerequisite, and accessibility-destination coverage (currently passing). See "Continue asset conversion" above for current totals. |
| done | Encyclopedia links | The surviving `www2.childrenofur.com/encyclopedia/` mirror replaces defunct primary-site links in the footer, map context menu, and entity chat links. |
| planned | Street layer art localization | Use official `glitch-locations` art/metadata to eliminate remaining retired remote dependencies. |
| done | Live bug triage 2026-08-28: opaque sprite backgrounds (systemic pipeline defect) | User-reported: metal rock rendered with a solid green background instead of transparent. Root cause: `ffdec -export frame` (used for every source SWF whose art sits directly on the main timeline rather than inside a named `DefineSprite`) always rasterizes against the SWF's `SetBackgroundColor` stage tag -- confirmed directly by reading the raw tag stream (metal rock's is `66 CC 99`) and by testing ffdec's `lastExportTransparentBackground` config flag, which has no effect on `-export frame` output (verified byte-identical before/after). This is inherent ffdec CLI behavior for main-timeline export, not a one-off mistake. Audited every PNG under `content/sprites/` and `coUclient/web/files/sprites/generated/converted/` with a PIL alpha-extrema script: found 442 file-copies (129 unique source SWFs, 253 manifest output rows -- every asset ever converted via `ffdec -export frame`, e.g. all crop/spice/drink/herb/tool item icons, several mining-tier rocks, dirt pile) with alpha extrema exactly (255,255), i.e. fully opaque, plus a handful with (254,255) that were the same defect with 1-2 stray antialiased pixels. Fixed all of them with a new post-export step: parse each source SWF's actual `SetBackgroundColor` tag byte-for-byte (no rendering), then soft chroma-key that exact color out of every exported frame with a distance-based alpha ramp plus color decontamination (unpremultiplying the background's contribution from partially-covered edge pixels, so antialiased edges don't carry a visible background-color fringe -- verified necessary: a naive alpha-only key left a visible purple halo on a saturated-purple-background asset). Rebuilt every affected icon/sprite-sheet from the keyed frames using the existing `build-sprite-sheet.py` slicing logic (same `sourceFrameRange` from `content/source-manifest.json`), overwrote both the `content/sprites/...` and `coUclient/.../converted/...` copies, and re-verified: 0/1062 PNGs now show opaque-only alpha, all reprocessed outputs' geometry (frameWidth/height/numFrames) matches the manifest exactly (one exception: `guano-sprite`'s manifest `sourceFrameRange` pointed at a nonexistent frame 2 -- a pre-existing manifest inconsistency unrelated to this bug, its own note says both icon/sprite reuse the sole frame 1; fixed by using frame 1 for both, matching the note). Visually spot-checked several reprocessed assets (cherry icon, metal rock, a light bulb, a firefly jar, a purple-ink scroll) at 4-8x with a solid preview background: clean edges, no fringing, and no accidental removal of genuine dark-purple ink-wash art on the scroll (confirmed those pixels are >55px color-distance from the background and were correctly left untouched as real foreground). This was not a code bug anywhere in `tools/build-sprite-sheet.py` or the entity `.dart` files -- purely an ffdec-export-step gap now covered by a documented, repeatable chroma-key step for any future `-export frame` conversion. `tools/validate-content.mjs` still passes (no geometry/manifest changes needed). |
| done | Live bug triage 2026-08-28: dirt piles "floating" investigation | User-reported: dirt piles appear to float above the ground, like trees did before this session's earlier ground-anchoring fix. Investigated root cause per the three-way hypothesis (seed-script math bug / stale in-memory server state overwriting a corrected DB seed / already fixed): confirmed `DirtPile` is NOT in `tools/seed-demo-world.mjs`'s `FLOATING_TYPES`/`AERIAL_TYPES` (i.e. it already gets the ground-anchored `groundLineY - jitter` formula, same as `WoodTree`/`MetalRock`/etc. from the earlier fix). Recomputed `position()` for all 1,645 live `DirtPile` rows in the running Postgres DB against the current script and found **0 mismatches** -- the DB already holds exactly what the current formula would generate, both for streets with real CAT422 geometry and the no-geometry fallback branch. Checked the client anchor math (`coUclient/lib/src/game/entities/plant.dart`: `y = map['y'] - height`, `height` from the loaded spritesheet image, same bottom-anchor pattern as NPCs) -- correct, no discrepancy from what trees use. Checked `dirt_pile-maturity_{1,2}.png` for transparent padding that could visually offset the anchor: every one of the 22 frames (11 x 2 maturities) has its opaque bounding box touching the full 71px frame height at the bottom, so there's no hidden gap between the sprite's visible base and its anchor line. Checked 5 currently-loaded streets (from live `game-server` logs) that hold a `DirtPile` placement: all loaded without any DirtPile-related error. Conclusion: no remaining code bug found for this entity specifically -- it was already covered by the earlier session's tree/rock/animal ground-anchoring fix (`FLOATING_TYPES`/`AERIAL_TYPES` design), and the DB already reflects the corrected formula with no evidence of the in-memory-staleness failure mode that caused a similar-looking bug earlier this session. As a defensive measure per the investigation checklist, re-ran `tools/seed-demo-world.mjs` (idempotent -- re-verified 1,645/1,645 `DirtPile` rows unchanged after reseeding) and did a clean `docker compose restart game-server` so no pre-fix in-memory entity state could persist stale values back to the DB on a future street unload; post-restart logs are clean (`[Init] Server started successfully`, `LocalPlayer` reconnected automatically, zero errors in the following ~15s window). If dirt piles still visibly float in-browser after this, it is not reproducible from server-side data/logic and would need an in-browser screenshot to diagnose further (e.g. a stale cached client bundle). |
| done | Live bug triage 2026-08-28: DemoWheat placeholder removed from the live world | User-reported: "Demo wheat" has no real game content behind it (no real item, no actions) unlike every other placed type. Confirmed: `coUserver/lib/entities/plants/demo_plants.dart`'s `DemoWheat` class is explicitly commented "intentionally non-harvestable," and `tools/seed-demo-world.mjs` still placed it (`REAL_TYPES`, `RARITY` 50%/maxCount 2, `REGION_THEME` entries for cave/ilmenskie/uralia2/kajuu/ix/hell, `RUNTIME_ASSET_ID`). Checked the file's own documented precedent first (per instruction): `DemoTree`/`DemoChicken` were already retired the same way once real `WoodTree`/`Chicken` art existed to supersede them in place. Searched for an equivalent real substitute for `DemoWheat` (option (a) in the task): no `wheat` folder exists anywhere under `tmp/glitch-items/`, and no `wheat` item exists in the item registry -- the closest existing things (`grain` item, `Garden` NPC's 13 vegetable crops) are unrelated, already-real, already-placed routes, not a wheat substitute. Converting a wholly new entity class from nothing was judged out of scope for a live triage pass (option (a) explicitly allowed skipping when no real equivalent exists), so went with option (b): removed `DemoWheat` from `RARITY`, every `REGION_THEME` entry, `REAL_TYPES`, and `RUNTIME_ASSET_ID` in `tools/seed-demo-world.mjs`, and added an explicit one-time `DELETE FROM street_entities WHERE id LIKE 'demo-%' AND type = 'DemoWheat'` (needed because the script's normal per-run cleanup DELETE is scoped to current `REAL_TYPES` membership, which no longer includes `DemoWheat`, so old rows would otherwise be silently orphaned rather than removed). Moved its `content/runtime-manifest.json` entry from the placed-assets list into `deliberatelyDeferred` (matching the `demo-chicken`/`demo-tree` entries' shape and reasoning) and corrected `content/placement-manifest.json`'s stale `$schema` description, which still named DemoWheat as a currently-placed stand-in. The `DemoWheat`/`demo_plants.dart` class itself is left in the codebase unreferenced, same as `DemoTree`/`DemoChicken`. Verified live: reseeded (2,111 pre-existing `DemoWheat` rows -> 0, confirmed via direct `SELECT count(*) FROM street_entities WHERE type = 'DemoWheat'`) and restarted `game-server` cleanly (see the dirt-pile row above for the same restart's clean-log verification). `tools/validate-content.mjs` still passes. |
| planned (recurred) | Pre-existing crash: `List<dynamic>` cast in quest favor rewards (see row above, unchanged) | Recurred again live during this triage pass: `game-server` was found to have restarted (`Unhandled exception: type 'List<dynamic>' is not a subtype of type 'List<QuestFavor>'` in `MetabolicsChange.trySetMetabolics`, reached via `Tree.pet`'s reward chain) while `LocalPlayer` was actively playing, independent of and not caused by any of this pass's 3 bug fixes. Left unfixed per this pass's scope (3 specific visual bugs only); still tracked by the existing row above. |

## Gameplay and progression

| Status | Work | Evidence / next verification |
| --- | --- | --- |
| done | Local starter quest route | Local-only Q1 seed uses an existing quest; Magic Rock null-choice path was fixed. |
| done | Quest completability audit | Verified 2026-08-28: traced all 12 quests in `coUserver/lib/quests/json/*.json` against the current codebase and `tools/seed-demo-world.mjs` placement, including full recipe-input chains and every quest's offer-to-player trigger. 11/12 are FULLY COMPLETABLE end-to-end (some via 2-3-tier recipe chains, e.g. Q1/Q12); 1 (Q7 "Snocone Joy") is PARTIALLY BLOCKED -- its `snocone_*` purchase requirement has no live source because `SnoConeVendingMachine` isn't in `seed-demo-world.mjs`'s placed-NPC list (only its own class exists, same shape as the already-fixed `Still`/`HoochRespawningItem` gap). Full per-quest detail in `POTENTIAL_TODO.md`'s "Quest completability audit" section. |
| planned | Place `SnoConeVendingMachine` in `tools/seed-demo-world.mjs` | Fixes Q7's blocked `getItem_snocone_*` requirement -- the class exists and is real (`coUserver/lib/entities/npcs/vendors/snoconevendingmachine.dart`) but is never instantiated in the live world. Confirm its constructor signature against `street.dart`'s reflective placement code first (same check already done for `Still`/`Crab`/the `HoochRespawningItem` family) before adding it to `VENDOR_NPC_TYPES`. |
| active | Item-tree recovery plan | `CONTENT_RECOVERY_PLAN.md` defines source provenance, accessibility destinations, and the rule that every runtime asset/item has a route or documented deferral. |
| planned | Import/translate item, recipe, vendor, quest, and achievement graph | Use CC0 original GameServerJS as reference data; add typed manifest plus prerequisite/reachability validation. |
| planned | Balanced synthetic progression | Once entity assets are local, seed animals/harvestables and make their rewards feed recipes, vendors, and quest prerequisites. |

## Deployment readiness

| Status | Work | Evidence / next verification |
| --- | --- | --- |
| planned | Split local and hosted auth configuration | Local bypass must be unavailable in deployed mode; retain Firebase-supported path. |
| planned | WebSocket/HTTP authentication and authorization | Required before any non-local deployment. Bind identity server-side and allowlist actions. |
| planned | Deterministic container builds | Pin toolchains/dependencies, add health checks/restarts, and bind local-only ports to localhost. |
| done | Sound policy | Per-region ambient music recovered 2026-08-28. The client/server per-street-\>hub-\>music-key trigger (`coUserver` `hubdata.json` `music` field -\> `/getMapData` -\> client `mapData.getSong()` -\> `Street.load()` -\> `audio.setSong()`) already existed and needed no changes. Added: 15 local `.ogg` tracks (12 from the YouTube playlist https://www.youtube.com/playlist?list=PLN0GhXnhPcciUtcwFG5MfdT284einO0k1 , all 23 of its videos fetched via `yt-dlp` and matched by title, 3 from the pre-existing `Glitch Soundtrack MP3.zip`'s "Soundscapes of Ur" album to fill region keys the playlist didn't cover) under `coUclient/web/files/sounds/music/`, keyed to match the existing `coUclient/web/files/json/music.json`/`hubdata.json` namespace exactly (`forest`, `ix`, `firebog`, `highlands`, `cave`, `hell`, `ilmenskie`, `uralia2`, `urwok`, `kajuu`, `kloroandhaoma`, `enchanted`, `ancestral`, `jal`, `nottis`). `SoundManager.loadSong()` (`coUclient/lib/src/systems/audio.dart`) now loads these local files directly when no SoundCloud token is configured, instead of only logging that music is disabled; SoundCloud remains the fallback path for any key without a local file. Full provenance, confidence, and every deliberately-unmatched key/track in `content/music-manifest.json`. Remaining gap: the `forest_slow` region (Bortola/Brillah/Haraiva/Karnata/Muufo hubs) has no confidently-matched track in either source and is left silent, same as before this change — needs either a human-identified source track or an explicit fallback decision. The playlist's "Wintry Place" track is a strong candidate for the already-reserved-but-unused `wintry` music.json key, but wiring it requires a `coUserver` `streetdata.json` edit (server data, outside this pass's client-only scope) — see `content/music-manifest.json`'s `unmatchedPlaylistTracks`. The original per-region multi-track rotation (arrays of tracks per hub) was not reintroduced; one track per key, matching the already-built single-scid-per-key architecture. |
| done | Version control / fork setup | Repo pushed to https://github.com/davidmanheim/ChildrenOfUr . `coUserver`/`coUclient`/`authServer` are git-subtree merges of real forks (davidmanheim/{coUserver,coUclient,authServer}, tracking ChildrenOfUr org upstream `dev`), preserving original commit history; local-revival changes sit in commits on top. See `LOCAL_SETUP.md` "Version control" for remote layout. `vendor/` stays plain vendored snapshots (not forked). |

## Verification checklist for each change

1. Rebuild/restart only the affected container(s).
2. Confirm the relevant HTTP/WebSocket endpoint is healthy.
3. Exercise the change in an actual browser session.
4. Inspect the game-server logs for errors for at least one reconnect/street change.
5. Update this tracker with the observed result and remaining dependency.
